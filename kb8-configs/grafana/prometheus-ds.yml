apiVersion: 1

datasources:
  - name: Prometheus
    type: prometheus
    url: http://prometheus-service.hydra.svc.cluster.local:9090
    access: proxy
    isDefault: true
    editable: false
    jsonData:
      httpMethod: POST
